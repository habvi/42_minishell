name: Minishell
on:
  push:
    branches: [ "dev" ]
  pull_request:
    branches: [ "dev" ]

  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Install python3.
        run: python3 -m pip install --upgrade pip setuptools

      - name: Install valgrind
        run: sudo apt-get -y install valgrind

      - name: make
        run: make

<<<<<<< HEAD:.github/workflows/minishell_pipe.yml
      - name: Install python3.
        run: python3 -m pip install --upgrade pip setuptools
      - name: Minishell multi pipe check.
        run: python3 .github/sh/minishell_pipe.py
=======
      - name: Run all test of MINISHELL.
        run: python3 .github/sh/run_all.py
>>>>>>> c513871 (update(yml): add install valgrind):.github/workflows/minishell_test.yml
