#include "ft_math.h"

size_t	ft_max(const size_t x, const size_t y)
{
	if (x >= y)
		return (x);
	return (y);
}

double	ft_max_double(const double x, const double y)
{
	if (x >= y)
		return (x);
	return (y);
}
