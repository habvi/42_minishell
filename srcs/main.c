#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>

static void	minishell(void)
{
	while (true)
	{
		// readline
		// tokenize
		// parse
		// exec
		printf("aaa\n");
	}
}

int	main(void)
{
	minishell();
	return (EXIT_SUCCESS);
}
